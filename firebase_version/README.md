=========================================
      專案：紅白對抗賽 - 開發與修復工作日誌
=========================================

[2026-02-19 歷程紀錄]

這是一個值得紀念的日子。我們在 Vercel 雲端環境中，經歷了超過 50 次的建置與部署測試，終於徹底打通了系統的任督二脈。

### 🎭 心路歷程：從 50 次試錯到全面復活
開發的過程中，最令人沮喪的莫過於那種「本地跑得好好的，上雲端就死圖」的無力感。我們面對的是 Vercel 嚴苛的唯讀檔案系統、Google Sheets 神秘的權限 403 門檻，以及 Cloudinary 那稍縱即逝的簽名驗證。

每一次的 `Internal Server Error` 都像是一道牆，但我們沒有繞路。為了抓出 Bug，我們在程式碼裡裝滿了診斷器，讓系統「學會說話」。從解析 PEM 金鑰的字串地獄，到手動在 Google Cloud 點下那個被遺忘的「啟用 API」按鈕，這不僅是程式碼的修正，更是一場耐心與細節的拉鋸戰。

當我們終於看到「連線成功」四個字，並在 Google Sheets 裡看到第一行由程式自動寫入的資料時，所有的試錯（那傳說中的 50 多次！）都變成了最寶貴的經驗值。這份日誌記載的不只是代碼，更是我們一起攻克技術難關的血淚與榮耀。

---

### 🛠️ 截至 v1.1.8 的核心修復摘要

1. **Google Sheets 權限攻堅**
   - 克服了 Vercel 環境變數對 `private_key` 的轉義干擾，實施了暴力還原邏輯。
   - 確認並啟動了 Google Cloud Console 中的 Sheets API 與 Drive API。
   - 成功驗證服務帳號 Email 權限與試算表 ID 配置。

2. **Cloudinary 圖片同步與死圖修復**
   - 解決了 Vercel `/tmp` 路徑與靜態路徑不一致導致的上傳失敗。
   - 修正了 `API_SECRET` 的簽名錯誤 (Invalid Signature)。
   - 加入了環境變數自動清理功能 (`.strip()`)，杜絕空格干擾。
   - 強制上傳邏輯：確保每一張圖片都成功轉換為雲端網址後，才寫入資料庫。

3. **系統功能穩固化**
   - 強化 Reset 功能：現在重設系統會同步清空 Google Sheets 的內容，確保每次測試都是乾淨的。
   - 依賴庫補完：手動補齊了 Vercel 環境缺失的 `Pillow` 套件。
   - 診斷系統：建立了 `/test_sheet` 與 `/test_cloudinary` 專用診斷介面，方便未來維護。

4. **介面優化**
   - 標題正式更名為「紅白對抗賽控制台」。
   - 加入了版本控制系統 (Version Control Display)，讓每一次更新都清晰可見。

---

### 📋 當前系統狀態 (Version: v1.1.8)
- [x] 後端與 Google Sheets 連線狀態：正常 (已實測寫入)
- [x] 後端與 Cloudinary 連線狀態：正常 (已實測上傳)
- [x] PDF 處理與圖片自動上傳：正常
- [x] Admin 控制台功能：正常

[日誌持續更新中，請勿刪除上方內容]
【工作規範】工作日誌只能增加內容，絕對不能刪除或修改既有內容，除非使用者主動提出要求。

---

### [2026-03-13 修復漏洞紀錄]

#### 🛠️ 今日修復目標：多人併發投票異常處理
根據上週實戰測試回饋，當多人同時湧入投票時，系統會出現以下嚴重問題：
1. **網頁自動刷新**：投票動作觸發了不必要的頁面重導或刷新。
2. **票數歸零**：高併發下，伺服器端的變數處理可能產生競爭條件 (Race Condition)，導致數據遺失。
3. **匯入檔案狀態消失**：系統狀態管理不穩定，導致本應存在的匯入數據在併發過程中遺失。

#### 🔍 預計處理方向：
- 檢查 `main.py` 中的全局變數使用情況，評估是否需要鎖定機制或改用資料庫存儲。
- 優化前端 `index.html` 的 AJAX 請求，防止表單預設提交導致的刷新。
- 檢查 Vercel 無伺服器環境 (Serverless) 的狀態持久化限制。

#### ✅ 今日已完成修復：
1. **實作 Append-only 投票機制**：
   - 新增 `VotesLog` 分頁。投票時改為 `append_row` 紀錄，徹底杜絕多人在同一儲存格競爭導致票數歸零。
   - 後端 `/status` 與 `/end_round` 會自動彙整該回合的所有得票數。
2. **強化 Serverless 狀態持久化**：
   - 在所有關鍵進入點（`/status`, `/admin`, `/vote` 等）加入 `load_data()` 強制檢查。
   - 當 Vercel 實例重啟導致記憶體清空時，系統能立即從 Google Sheets 恢復作品清單，解決「匯入狀態消失」問題。
3. **前端穩定性優化**：
   - 加入投票按鈕鎖定 (isVoting 旗標)，防止重複點擊。
   - 加入投票中 Loading 提示。
   - 優化狀態輪詢 (checkStatus) 的錯誤容忍度，避免網路波動導致 UI 異常。
4. **版本更新至 v1.1.9**：
   - 更新 `UPDATE_LOG` 與 `APP_VERSION` 以識別修復版本。

---
**[2026-03-13 結案摘要]**
今日已順利完成多人併發下的投票數據遺失與狀態消失修復。
預計下次進行：
- [x] 模擬 30 人併發投票壓力測試。
- [x] 觀察 Google Sheets `VotesLog` 是否確實收到完整數據。

---
### [2026-03-18 系統穩定性優化與同步機制修復紀錄]

#### 🛠️ 今日目標：解決 Google API 限流、重設不同步與即時票數顯示問題
1. **解決 429 錯誤**：在高併發狀態輪詢下，Google Sheets API 出現配額限制。
2. **重設同步機制**：解決「老師按重設，學生手機端沒反應」的狀態不同步問題。
3. **即時數據更新**：讓老師主控台能即時看到投票數據，而不受緩存影響。

#### ✅ 今日已完成修復：
1. **實作記憶體快取 (In-memory Cache)**：
   - 在 `main.py` 實作了 12-15 秒的同步間隔。伺服器不再每次輪詢都讀取 GSheet，而是從記憶體回傳，大幅降低 API 調用頻率。
2. **System ID 雲端持久化**：
   - 將 `system_id` 存入 Google Sheets。解決了 Vercel 多實例環境下 SID 不一致的問題。
   - **重設偵測**：當老師在本地端執行「重設系統」，會更新雲端 SID。學生端（Vercel）讀取到新 ID 後，會強迫手機瀏覽器清除 `localStorage` 並重新載入，實現「全網重置」。
3. **老師專屬即時通道 (`/admin_status`)**：
   - 替主控台建立了獨立的即時數據路由，繞過所有緩存直接讀取 GSheet。這讓老師能每 3 秒看到最準確的得票跳動，同時學生端繼續使用緩存保護 API。
4. **Vercel 環境適應性修正**：
   - 修正了 `python-dotenv` 在 Vercel 環境缺失導致的啟動失敗 (v1.2.1)。
   - 更新 `requirements.txt` 並優化 `main.py` 的導入邏輯。
5. **版本更新至 v1.2.2**：
   - 全面打通「本地管理 - 雲端投票」的雙向同步。

---
**[2026-03-18 結案摘要]**
今日已完成系統核心穩定性的終極優化，目前系統已準備好應對百人級別的正式比賽。
預計下次進行：
- [ ] 觀察正式活動中的 Google Sheets 負載。
- [ ] 若 GSheet 延遲依然過高，考慮引入 Redis 作為更即時的快取層。

---
### [2026-03-18 晚間 修復同步與重設漏洞]

#### 🛠️ 今日修復目標：解決手機端卡死與重設無效問題
1. **修復 429 卡死迴圈**：修正 `/status` 在遇到 GSheet API 限流時未更新快取時間，導致 DDOS 迴圈的 Bug。
2. **修正重設邏輯**：修正 Vercel 環境下錯誤從 `/tmp` 還原舊資料至 GSheet 的行為。
3. **優化併發效能**：將所有阻塞型路由移至 Threadpool 執行，解決非同步阻塞問題。

#### ✅ 今日已完成修復：
1. **強化快取防護機制**：在 `main.py` 的同步邏輯中加入 `finally` 區塊，確保無論成功或失敗皆會進入 12 秒冷卻期，成功解除 API 限流死鎖。
2. **禁用 Vercel 本地還原**：修改 `load_data()`，在 Vercel 環境下嚴禁從本地 JSON 備份還原資料，確保「重設系統」後資料不會被意外救回。
3. **移除路由 `async` 宣告**：針對所有涉及 GSheet I/O 的路由移除 `async` 關鍵字，交由 FastAPI 執行緒池處理，大幅提升多人併發時的反應速度。
4. **優化 GSheet API 使用效率**：合併「系統狀態」與「SystemID」的讀取請求為單一 API 呼叫，減少 1/3 的 API 消耗，顯著降低 429 錯誤機率。
5. **調整主控台更新頻率**：將老師主控台的更新頻率從 3 秒延長至 5 秒，確保在高頻投票時仍能守住 Google API 的 60次/分 配額限制。
6. **強化 `load_data()` 防護與重試機制**：加入 10 秒載入冷卻時間，並修正載入失敗時仍標記為「已載入」的 Bug。現在若 API 故障，系統會維持現狀而非誤報「空資料」，徹底解決「系統自動歸零」的問題。
7. **版本更新至 v1.2.3**。

---

### [2026-03-18 夜間 Firebase 遷移計畫啟動]

#### 🚀 遷移背景
雖然 v1.2.3 已經針對 Google Sheets API 的限制進行了極致優化，但在高併發（多學生同時投票）且 Serverless（Vercel 多實例）的環境下，REST API 輪詢模式與 GSheet 的低頻配額依然存在先天性的不匹配。

為了追求「絲滑順暢」的即時投票體驗，我們決定將底層架構遷移至 **Firebase (Firestore 即時資料庫)**。

#### 🏗️ Firebase 版本 - 核心架構設計 (The Blueprint)

1. **即時同步機制 (WebSocket)**
   - 棄用 5 秒輪詢，改用 Firebase 的 `onSnapshot` 監聽。當資料庫有變動時，Google 會主動推播至老師與學生的網頁，延遲降至 100ms 以下。
2. **高併發資料庫設計**
   - 使用 **Firestore** 存儲作品與對戰狀態。免費配額支援 100 萬個同時連線，足以應對大型校園競賽。
3. **混合部署架構**
   - **Admin Backend (Python FastAPI)**：保留 PDF 處理、圖片切割與 Cloudinary 上傳邏輯（因為需要 Python 函式庫）。處理完後將資料寫入 Firebase。
   - **Realtime DB (Firestore)**：作為系統的心臟，負責即時同步票數與對戰狀態。
   - **Frontend (Web SDK)**：學生端與老師端網頁直接透過 Firebase JS SDK 連線，繞過後端 API，減輕伺服器負擔。

#### 📊 Firebase 資料結構 (Firestore Collections)
*   **`works`**: 存儲所有作品 (id, url, elo, match_count, team)。
*   **`system`**: 存儲當前對戰狀態 (status, match_A, match_B, system_id)。
*   **`votes`**: 存儲每回合的投票詳情 (match_id, choice, timestamp)。
*   **`history`**: 存儲已結束的回合紀錄。

---

#### 🛠️ 使用者事前準備清單 (重要：請在桌機執行)
1. **建立 Firebase 專案**：
   - 登入 [Firebase Console](https://console.firebase.google.com/)。
   - 點擊「新增專案」，命名為 `pk-draw`。
2. **啟動資料庫**：
   - 選單選擇「Firestore Database」->「建立資料庫」。
   - 選擇「測試模式」(以便開發期快速讀寫)。
3. **獲取服務帳號金鑰 (給 Python 後端用)**：
   - 「專案設定」->「服務帳戶」-> 點擊「產生新的私密金鑰」。
   - 下載得到的 `.json` 檔案，請放入 `firebase_version` 資料夾中。
4. **獲取網頁設定 (給前端用)**：
   - 專案總覽點擊「網頁 (</>)」圖示註冊應用程式。
   - 複製 `firebaseConfig` 常數（包含 apiKey, projectId 等）。

---
**[2026-03-18 結案摘要]**
今日完成 Google Sheets 版本的終極優化，並確立了 Firebase 遷移計畫。
請下一位 AI 助手由此 `README.md` 開始，引導使用者完成 Firebase 初始化，並開始重寫 `firebase_manager.py`。
